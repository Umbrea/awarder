import React from 'react'

const Publish = () => (
  <div className="publish">
    <div className="name">编辑推送信息</div>
    <textarea className="pub-area">
    </textarea>
  </div>
)

export default Publish
